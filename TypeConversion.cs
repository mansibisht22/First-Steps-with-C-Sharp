using System;
class TypeConversion
{
    static void Main()
    {
        int count = 1;
        string name = "Mansi";
        int total = 100000;

        Console.writeLine("This is " + name + "'s" + count + "event of of the " + total + "she will host");

    }
}